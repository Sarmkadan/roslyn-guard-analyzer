# FileSystemHelper

A static utility class providing common filesystem operations for locating, reading, writing, and inspecting files and directories within a C# codebase or solution. Designed to abstract repetitive filesystem tasks, particularly for Roslyn-based tooling that analyzes or modifies source code.

## API

### `FindCSharpFiles`
